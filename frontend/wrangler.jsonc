{
  // Cloudflare Workers config for the static site (assets-only worker).
  // Deploy with: npx wrangler deploy  (run from frontend/ after `npm run build`)
  "name": "iciar-roca-de-la-esperanza",
  "compatibility_date": "2026-08-22",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
